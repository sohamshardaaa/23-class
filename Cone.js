class Cone {
    constructor(x,y,sides,size){
        var options = {
            'restitution':1,
            'friction':0.8,
            'density':1.0
        }
        
    }
    display(){
    
      //rectMode(CENTER);
      fill("grey");
      //rect(0, 0, this.width, this.height);
      pop()
      
      fill("grey");
      strokeWeight(2);
      
     // regularPolygon(720, 200, 3, 50);
    }
}